{
  "name": "nps-hopefarm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --port 3000 --open",
    "build": "vite build && cp -r js dist/ && cp vercel.json dist/ && mkdir -p dist/assets/images && cp assets/images/* dist/assets/images/",
    "preview": "vite preview",
    "start": "vite --port 3000"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  }
}
